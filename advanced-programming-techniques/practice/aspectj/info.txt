Salve, premessa che ho letto velocemente l'articolo `Aspect-Oriented Programming del 1997` in particolare nella sezione `8.2` viene detto: `"meta-languages are lower-level aspect languages whose join points are the “hooks” that the reflective system provides."`. Ho trovato questo package `https://docs.oracle.com/middleware/11119/jdev/api-reference-esdk/javax/ide/util/MetaClass.html` in generle tutta la `javax.ide.*` non capisco perchè non ci sia nella documentazione ufficile e se possa essere utile per questo --> in ogni modo, stando a quanto detto nell'articolo, implementando un sistema di meta-classi in java non si puo'sostituire completamente l'utilizzo di aspectJ. Quello che dico sarebbe di utilizzare direttamente `javac/java` e avere un comportamento analogo a quello degli aspetti.
